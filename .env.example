# Hardware Intel Bot — cp .env.example .env and fill in real values.
# .env is gitignored; never commit real tokens.

DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_ID=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional — defaults to hardware_intel.db in the project root
HARDWARE_INTEL_DB_PATH=
